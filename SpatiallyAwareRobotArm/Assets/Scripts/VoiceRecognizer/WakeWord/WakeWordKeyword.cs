using UnityEngine;

namespace SA_XARM.WakeWord
{
    /// <summary>
    /// WakeWordKeyword is disabled to avoid conflict with DictationRecognizer.
    /// </summary>
    public class WakeWordKeyword : MonoBehaviour
    {
        // This component is intentionally left empty to remove dependency on KeywordRecognitionSubsystem.
    }
}
